# 백엔드(Spring Boot) API 베이스 URL — 서비스 테스트(②) 단계 전환 시 설정
# 로컬 docker-compose 기준 spring 서비스 host 포트는 8080 (backend/docker-compose.yml 참고)
# 사용법: 이 파일을 .env.live로 복사한 뒤 `npm run dev:live`로 실행
VITE_API_BASE_URL=http://localhost:8080
